# Website 0.1.2

Introduce Picberry on the homepage with a concise product overview and real Library and Preview screenshots. The website remains static and uses the existing Privacy and Support routes.

## Homepage overview

- Status: complete; source implementation and local verification finished. Publication outcome is recorded in CODEX-40.
- Task and agent: CODEX-40; coordinating Codex agent /root. A read-only screenshot audit was delegated to /root/homepage_screenshot_audit.
- User-visible changes: a larger product introduction, Library and Preview screenshots, short browsing, Albums/Favorites, People, and slideshow descriptions, and a compact local-processing statement with a Privacy link.
- Affected files: homepage HTML, homepage-scoped rules in `site/assets/site.css`, two local WebP assets, screenshot provenance, public-page specification, README, website package version, and this changelog.
- Contracts/data/build: static HTML/CSS, no JavaScript, remote image/font loads, forms, cookies, analytics, new dependencies, or backend. Existing legal routes, content, email, icon, and publication workflow remain unchanged. No desktop app files or version are modified.
- Legacy removed: the homepage's two-row link-only body; old home-only style rules are replaced by the product overview styles. Legal-page styling is retained.
- Legacy intentionally retained: existing Privacy/Support/404 documents and system-font stack; latest successful captures from 0.1.267 are used because later capture attempts did not complete. Screenshot refresh belongs to a future application capture task.
- Verification run: `npm run check`, `npm run build`, and `git diff --check` passed. Browser inspection covered desktop at 1280 px and mobile at 390 px, including both screenshots and lower content; DOM overflow checks also passed at 320 px and 768 px. Privacy and Support navigation succeeded. Both WebP dimensions and hashes match provenance; source hashes match the application manifest. A separate read-only review found no factual, private-name/path, metadata, static-contract, or legal-page regression.
- Verification not run: desktop application tests/builds and App Store release gates, because this task changes only the standalone website. GitHub Pages publication uses the existing workflow and is verified against the committed head before user handoff.
- Risks: captures are a previous successful application build, not evidence of current-build or App Store readiness. Settings screenshots containing a personal local path are excluded.
- Overlap: the application checkout contains unrelated ongoing work; it is read-only for this task. No other repositories or GitHub Desktop entries are changed.
