# Website 0.1.1

Use Picberry as the public identity throughout the website. The support and privacy contact remains the owner-provided email.

## Public identity correction

- Status: complete.
- Task and agent: CODEX-39; coordinating Codex agent.
- Scope: replace the personal name in all four footers and the Privacy and Support introductions; redact the same name from the initial changelog and Git author/committer metadata; preserve Picberry and the existing contact email.
- Affected files: the four HTML pages, `package.json`, `AGENTS.md`, `README.md`, `docs/specs/public-pages.md`, and website changelogs.
- Contracts and data: public identity is Picberry, with `milanvandal@gmail.com` as contact. Routes, data-handling descriptions, branding assets, and site behavior are unchanged.
- Legacy removed: personal-name references from current tracked files and the reachable branch history. The two initial commits retain their other content and ordering after targeted redaction; all three commits in the corrected branch were scanned, including every blob and author/committer header. Both previous Pages artifact archives were deleted and their run artifact counts verified as zero.
- Legacy retained: third-party caches and unreachable GitHub objects are outside the repository's direct control. Do not claim universal erasure from external systems.
- Verification run: read-only scan of all tracked files and both initial commits; independent audit confirmed the complete set of name references. npm check/build passed; home, Privacy, Support, and 404 were inspected locally, including 390 px mobile rendering.
- Publication verification: GitHub Actions run 34821690383 succeeded for the corrected branch. Public home, Privacy, and Support returned 200; a nested unknown route returned the custom 404. All four responses contained the Picberry copyright and no personal name. The open browser tab was refreshed and inspected.
- Risks: history redaction changed commit IDs. The remote update used an explicit force-with-lease against the verified previous head. Old third-party caches and unreachable remote objects may remain; the public site and reachable repository history are verified.
- Overlap: only the standalone website repository is modified; no desktop application changes.
