{
  "name": "picberry-site",
  "version": "0.1.0",
  "private": true,
  "description": "Public privacy policy and support for Picberry.",
  "scripts": {
    "check": "python3 scripts/check.py",
    "build": "python3 scripts/build.py",
    "preview": "python3 -m http.server 4173 --bind 127.0.0.1 --directory _site"
  }
}
